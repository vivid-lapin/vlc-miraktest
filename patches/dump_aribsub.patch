diff --git a/modules/codec/arib/aribsub.c b/modules/codec/arib/aribsub.c
index 46272ea175..2e40323a1f 100644
--- a/modules/codec/arib/aribsub.c
+++ b/modules/codec/arib/aribsub.c
@@ -27,6 +27,7 @@
 #include <vlc_common.h>
 #include <vlc_plugin.h>
 #include <vlc_codec.h>
+#include <vlc_strings.h>
 
 #ifdef HAVE_ARIBB24
  #include "substext.h"
@@ -34,6 +35,8 @@
  #include <aribb24/decoder.h>
 #endif
 
+#include "../../demux/mpeg/timestamps.h"
+
 //#define DEBUG_ARIBSUB 1
 
 /*****************************************************************************
@@ -173,14 +176,18 @@ static int Decode( decoder_t *p_dec, block_t *p_block )
         return VLCDEC_SUCCESS;
     }
 
+    char *b64 = vlc_b64_encode_binary(p_block->p_buffer, p_block->i_buffer);
+    msg_Dbg(p_dec, "arib_data [%s][%" PRId64 "]", b64, p_block->i_pts);
+    free(b64);
+
     arib_parser_t *p_parser = arib_get_parser( p_sys->p_arib_instance );
     arib_decoder_t *p_decoder = arib_get_decoder( p_sys->p_arib_instance );
     if ( p_parser && p_decoder )
     {
-        arib_parse_pes( p_parser, p_block->p_buffer, p_block->i_buffer );
-        subpicture_t *p_spu = render( p_dec, p_parser, p_decoder, p_block );
+        arib_parse_pes(p_parser, p_block->p_buffer, p_block->i_buffer);
+        /*subpicture_t *p_spu = render( p_dec, p_parser, p_decoder, p_block );
         if( p_spu != NULL )
-            decoder_QueueSub( p_dec, p_spu );
+            decoder_QueueSub( p_dec, p_spu );*/
     }
 
     block_Release( p_block );
diff --git a/modules/demux/mpeg/ts_psi.c b/modules/demux/mpeg/ts_psi.c
index 054a019bd4..6e6dbef22d 100644
--- a/modules/demux/mpeg/ts_psi.c
+++ b/modules/demux/mpeg/ts_psi.c
@@ -399,7 +399,7 @@ static ts_standards_e ProbePMTStandard( const dvbpsi_pmt_t *p_dvbpsipmt )
             {
                 const uint16_t i_data_component_id = GetWBE(p_dr->p_data);
                 if( ( i_data_component_id == 0x0008 &&
-                      PMTEsHasComponentTagBetween( p_dvbpsies, 0x30, 0x37 ) ) ||
+                      PMTEsHasComponentTagBetween( p_dvbpsies, 0x30, 0x88 ) ) ||
                     ( i_data_component_id == 0x0012 &&
                       PMTEsHasComponentTagBetween( p_dvbpsies, 0x87, 0x88 ) ) )
                     return TS_STANDARD_ARIB;
@@ -1078,7 +1078,7 @@ static void PMTSetupEs0x06( demux_t *p_demux, ts_stream_t *p_pes,
             /* See STD-B10 Annex J, table J-1 mappings */
             const uint16_t i_data_component_id = GetWBE(p_dr->p_data);
             if( i_data_component_id == 0x0008 &&
-                PMTEsHasComponentTagBetween( p_dvbpsies, 0x30, 0x37 ) )
+                PMTEsHasComponentTagBetween( p_dvbpsies, 0x30, 0x88 ) )
             {
                 es_format_Change( p_fmt, SPU_ES, VLC_CODEC_ARIB_A );
                 p_fmt->psz_language = strndup ( "jpn", 3 );
